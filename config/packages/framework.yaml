framework:
    secret: '%env(APP_SECRET)%'
    session: true
    csrf_protection:
        stateless_token_ids: []

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
