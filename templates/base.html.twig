<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}AquaBiome{% endblock %}</title>
    
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/base.css') }}">
    {% endblock %}

    {% block javascripts %}
        {{ importmap('app') }}
    {% endblock %}
</head>
<body>

    <div class="smartphone-container">
        
        <header class="main-header">
            <div class="header-container">
                {# Logo #}
                <img src="{{ asset('image/logo_aquabiome.png') }}" class="header-logo" alt="Logo">
                
                {# Nom de la page #}
                <div class="active-tab-pill">
                    <span>🏠</span>
                    <span>{% block page_name %}Accueil{% endblock %}</span>
                </div>

                {# Profil et Menu #}
                <div style="display: flex; align-items: center; gap: 10px;">
                    <span style="font-size: 1.5rem;">👤</span>
                    <button class="nav-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="{{ 'var(--blue)' }}" stroke-width="2">
                            <line x1="3" y1="6" x2="21" y2="6"></line>
                            <line x1="3" y1="12" x2="21" y2="12"></line>
                            <line x1="3" y1="18" x2="21" y2="18"></line>
                        </svg>
                    </button>
                </div>
            </div>
        </header>

        {# Contenu variable de tes pages #}
        <main>
            {% block body %}{% endblock %}
        </main>

    </div>

</body>
</html>